# Secrets — never commit
.env
.env.local
auth.json
**/auth.json
**/.env

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Hermes runtime (if someone clones into a live profile dir by mistake)
state.db
state.db-*
sessions/
memories/
logs/
cache/
backups/
gateway.pid
gateway_state.json
processes.json
*.log
